{
    "functions": {
        "api/note": {
            "handler": "app.ts"
        }
    },
    "routes": [
        {
            "src": "/v1/note/(.*)",
            "dest": "/api/note/:1"
        },
        {
            "src": "/(.*)",
            "dest": "/api/:1"
        }
    ]
}